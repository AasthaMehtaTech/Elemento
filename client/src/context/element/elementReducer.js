import {
  GET_ELEMENTS,
  SEARCH_ELEMENT,
  CLEAR_ELEMENTS,
  SET_LOADING,
} from "../types";

export const elementReduer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
